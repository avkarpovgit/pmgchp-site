---
// Карточка раздела на главной. Две формы:
//  - href задан → вся карточка <a> (заголовок без ссылки);
//  - href не задан → <div>, ссылки только в meta и под-ссылках watch (заголовок без ссылки).
interface WatchLink {
  label: string;
  href: string;
}
interface Props {
  title: string;
  href?: string;
  meta?: string;
  metaHref?: string;
  watch?: WatchLink[];
  class?: string;
  target?: string;
  rel?: string;
}
const { title, href, meta, metaHref, watch, class: cls, target, rel } = Astro.props;
const Wrapper = href ? 'a' : 'div';
---

<Wrapper class:list={['section-card', cls]} href={href} target={target} rel={rel}>
  <h3>{title}</h3>
  <p><slot /></p>
  {meta && <span class="card-meta">{metaHref ? <a href={metaHref}>{meta}</a> : meta}</span>}
  {
    watch && watch.length > 0 && (
      <div class="card-watch">
        {watch.map((w) => (
          <a href={w.href}>{w.label}</a>
        ))}
      </div>
    )
  }
</Wrapper>
